{{- $architecture := or .architecture "arm64" -}}
{{- $device := or .device "google-sunfish" }}

architecture: {{ $architecture }}

actions:
  - action: apt
    recommends: false
    description: Install device-specific packages for Plasma
    packages:
      - firefox-esr-mobile-config
